using Microsoft.UI.Xaml;

namespace SipPOS;

/// <summary>
/// Represents the window that performs application startup checks.
/// </summary>
public sealed partial class AppStartupCheckWindow : Window
{
    /// <summary>
    /// Initializes a new instance of the <see cref="AppStartupCheckWindow"/> class.
    /// </summary>
    public AppStartupCheckWindow()
    {
        this.InitializeComponent();
    }
}
