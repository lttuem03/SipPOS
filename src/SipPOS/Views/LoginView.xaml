<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="SipPOS.Views.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SipPOS.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        Width="700"
        Height="300">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <NavigationView
            Name="loginNavigationView"
            PaneDisplayMode="Top"
            IsBackButtonVisible="Collapsed"
            IsSettingsVisible="False"
            SelectionChanged="loginNavigationView_SelectionChanged">
            <NavigationView.MenuItems>
                <NavigationViewItem Content="Đăng nhập nhân viên" Tag="StaffLogin" Icon="Contact"/>
                <NavigationViewItem Content="Đăng nhập cửa hàng" Tag="StoreLogin" Icon="Shop"/>
            </NavigationView.MenuItems>
        </NavigationView>

        <Frame x:Name="loginNavigationFrame" Grid.Row="1"/>
    </Grid>
</Page>
